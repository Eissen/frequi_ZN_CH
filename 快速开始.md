# FreqUI 本地开发 - 快速开始

## ✅ 环境已配置完成

所有必要的环境已经配置好了：
- ✅ Node.js 20.19.5 已安装
- ✅ pnpm 10.22.0 已安装
- ✅ 项目依赖已安装
- ✅ CORS 已配置

## 🚀 立即开始

### 方法一：使用启动脚本（推荐）

```bash
cd /home/ec2-user/frequi
./start-dev.sh
```

### 方法二：手动启动

```bash
cd /home/ec2-user/frequi
source ~/.bashrc  # 确保 pnpm 在 PATH 中
pnpm run dev
```

## 🌐 访问界面

启动后，在浏览器中打开：
- http://127.0.0.1:3000
- 或 http://localhost:3000

## 📝 开始汉化

### 快速汉化示例

1. **打开要汉化的组件：**
   ```bash
   cd /home/ec2-user/frequi
   # 使用你喜欢的编辑器
   vim src/components/ftbot/BotControls.vue
   ```

2. **查找并替换英文文本：**
   - 查找：`'Stop Bot'`
   - 替换为：`'停止机器人'`

3. **保存文件，浏览器会自动刷新显示中文**

### 详细汉化指南

查看以下文件获取完整指南：
- `开发环境配置和汉化指南.md` - 完整的开发环境说明
- `汉化示例_BotControls.md` - 具体的汉化示例

## 🔧 常用命令

```bash
# 启动开发服务器
pnpm run dev

# 构建生产版本
pnpm run build

# 代码检查
pnpm run lint

# 类型检查
pnpm run typecheck
```

## 📂 项目位置

- **项目目录：** `/home/ec2-user/frequi`
- **配置文件：** `/home/ec2-user/ft_userdata/user_data/config.json`
- **Freqtrade 数据：** `/home/ec2-user/ft_userdata`

## ⚠️ 重要提示

1. **确保 freqtrade 正在运行：**
   ```bash
   cd /home/ec2-user/ft_userdata
   docker compose up -d
   ```

2. **CORS 配置：**
   已在 `config.json` 中配置了 CORS，允许 `http://localhost:3000` 和 `http://127.0.0.1:3000` 访问。

3. **保存你的修改：**
   建议创建 git 分支保存你的汉化工作：
   ```bash
   cd /home/ec2-user/frequi
   git checkout -b zh-cn-localization
   git add .
   git commit -m "添加中文汉化"
   ```

## 🎯 下一步

1. 启动开发服务器
2. 选择一个组件开始汉化（建议从 `BotControls.vue` 开始）
3. 逐步汉化其他组件
4. 测试所有功能确保正常

## ❓ 遇到问题？

查看 `开发环境配置和汉化指南.md` 中的"常见问题"部分。

